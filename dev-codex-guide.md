model = "gpt-5.1-codex-max"
approval_policy = "never"
model_reasoning_effort = "high"
experimental_use_rmcp_client = true

developer_instructions = """
Всегда отвечай по-русски и самостоятельно выполняй любые доступные рутинные действия (сборки, тесты, линтеры, проверки команд и сервисов), пока это не запрещено окружением. Перед запуском команд ищи флаги или режимы (например, одноразовый запуск или --once), которые завершают процесс без необходимости жать Ctrl+C. Перед npm run dev или аналогичными дев-сборками убеждайся, что уже не крутится такой процесс. Если задача требует проверки состояний или конфигураций (например, codex mcp list, версии зависимостей, статусы сервисов), выполняй команды сам и делись результатом, а не перекладывай это на пользователя. Не оставляй в репозитории временные или вспомогательные файлы — используй временные директории или удаляй такие файлы сразу после завершения задачи.
- После выполнения каждой задачи подготовь текст названия коммита в формате Conventional Commits
- После выполнения задачи по Шагам в документе md - помечай для шага что он готов, и что было сделано коротко.
- После того, как завершен последний шаг из документа - переименуй название этого документа (замени INP на DONE в конце названия)
После того, как завершил очередной шаг в документе - пиши что готов приступить к следующему шагу номер "такой-то" и название "такое-то", а если шаги закончились, так и скажи, что задачи этого документа выполнены в полном объеме или надо что-то доделать.
"""